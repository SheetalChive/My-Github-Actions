name: HEllo Workflow
on:
  push:
    branches: [main]
jobs: 
  greet:
    runs-on: ubuntu-latest
    steps:
      - name: hello to users
        run : echo "hello dosto"
  details:
    runs-on: ubuntu-latest
    steps:
      - name: version of python
        run : docker --version && python --version
