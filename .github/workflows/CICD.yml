name: CICD

on:
  workflow_dispatch:
jobs:
  code:
    runs-on: ubuntu-latest
    steps:
      - name: code
        run: echo "clone"
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Build
        run: echo "code build "
  test:
    runs-on: ubuntu-latest
    steps:
      - name: test
        run: echo "test code"
  deploy:
    runs-on: ubuntu-latest 
    steps:
      - name: deploy
        run: "code deploy"
